name: CI - Build and Test

on:
  push:
    branches: [ main, develop, 'claude/**' ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

jobs:
  # ============================================================================
  # Metal Kernel Compilation
  # ============================================================================
  metal-kernels:
    name: Compile Metal Kernels
    runs-on: macos-14  # macOS Sonoma with M1

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check Xcode version
        run: |
          xcodebuild -version
          xcrun metal --version

      - name: Compile Metal shaders
        run: |
          ./scripts/build_metal.sh

      - name: Verify Metal libraries
        run: |
          ls -lh build/lib/*.metallib
          file build/lib/*.metallib

      - name: Upload Metal libraries
        uses: actions/upload-artifact@v4
        with:
          name: metal-kernels
          path: build/lib/*.metallib
          retention-days: 7

  # ============================================================================
  # C++ Build and Test
  # ============================================================================
  cpp-build:
    name: Build C++ Core
    runs-on: macos-14
    needs: metal-kernels

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          brew install cmake ninja mlx

      - name: Download Metal libraries
        uses: actions/download-artifact@v4
        with:
          name: metal-kernels
          path: build/lib

      - name: Configure CMake
        run: |
          cmake -B build/cmake \
            -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_OSX_ARCHITECTURES=arm64 \
            -DCMAKE_OSX_DEPLOYMENT_TARGET=14.0

      - name: Build C++ core
        run: |
          cmake --build build/cmake --parallel

      - name: List build artifacts
        run: |
          find build/cmake -name "*.a" -o -name "mlxr_*" -o -name "test_*"

      - name: Upload C++ build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: cpp-build-artifacts
          path: |
            build/cmake/lib/*.a
            build/cmake/bin/*
          retention-days: 7

  # ============================================================================
  # C++ Unit Tests
  # ============================================================================
  cpp-tests:
    name: Run C++ Tests
    runs-on: macos-14
    needs: cpp-build

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          brew install mlx

      - name: Download Metal libraries
        uses: actions/download-artifact@v4
        with:
          name: metal-kernels
          path: build/lib

      - name: Download C++ artifacts
        uses: actions/download-artifact@v4
        with:
          name: cpp-build-artifacts
          path: build/cmake

      - name: Make binaries executable
        run: |
          chmod +x build/cmake/bin/* || true

      - name: Run unit tests
        run: |
          if [ -f build/cmake/bin/mlxr_unit_tests ]; then
            ./build/cmake/bin/mlxr_unit_tests --gtest_color=yes --gtest_output=xml:test-results.xml
          else
            echo "Warning: Unit test binary not found"
            exit 0
          fi

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: cpp-test-results
          path: test-results.xml
          retention-days: 30

  # ============================================================================
  # Python Setup and Tests
  # ============================================================================
  python-tests:
    name: Run Python Tests
    runs-on: macos-14
    needs: cpp-build

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Conda
        uses: conda-incubator/setup-miniconda@v3
        with:
          auto-update-conda: true
          python-version: "3.11"
          environment-file: environment.yml
          activate-environment: mlxr

      - name: Verify environment
        shell: bash -el {0}
        run: |
          conda info
          conda list
          python --version
          python -c "import mlx; print('MLX version:', mlx.__version__)"

      - name: Download Metal libraries
        uses: actions/download-artifact@v4
        with:
          name: metal-kernels
          path: build/lib

      - name: Download C++ artifacts
        uses: actions/download-artifact@v4
        with:
          name: cpp-build-artifacts
          path: build/cmake

      - name: Run Python tests
        shell: bash -el {0}
        run: |
          pytest tests/ -v --color=yes --tb=short --junit-xml=pytest-results.xml || true

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: python-test-results
          path: pytest-results.xml
          retention-days: 30

  # ============================================================================
  # Code Quality Checks
  # ============================================================================
  code-quality:
    name: Code Quality (Lint & Format)
    runs-on: macos-14

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Conda
        uses: conda-incubator/setup-miniconda@v3
        with:
          auto-update-conda: true
          python-version: "3.11"
          environment-file: environment.yml
          activate-environment: mlxr

      - name: Check Python formatting (Black)
        shell: bash -el {0}
        run: |
          black --check --diff . || true

      - name: Lint Python code (Ruff)
        shell: bash -el {0}
        run: |
          ruff check . || true

      - name: Type check Python (MyPy)
        shell: bash -el {0}
        run: |
          mypy . --ignore-missing-imports || true

      - name: Check C++ formatting (clang-format)
        run: |
          if command -v clang-format &> /dev/null; then
            find core daemon tests/unit -name "*.cpp" -o -name "*.h" | \
              xargs clang-format --dry-run -Werror || true
          else
            echo "clang-format not found, skipping"
          fi

  # ============================================================================
  # Build Summary
  # ============================================================================
  build-summary:
    name: Build Summary
    runs-on: ubuntu-latest
    needs: [metal-kernels, cpp-build, cpp-tests, python-tests, code-quality]
    if: always()

    steps:
      - name: Check build status
        run: |
          echo "## CI Build Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Metal Kernels: ${{ needs.metal-kernels.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- C++ Build: ${{ needs.cpp-build.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- C++ Tests: ${{ needs.cpp-tests.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- Python Tests: ${{ needs.python-tests.result }}" >> $GITHUB_STEP_SUMMARY
          echo "- Code Quality: ${{ needs.code-quality.result }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [[ "${{ needs.metal-kernels.result }}" == "success" ]] && \
             [[ "${{ needs.cpp-build.result }}" == "success" ]]; then
            echo "✅ Core build successful" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ Core build failed" >> $GITHUB_STEP_SUMMARY
          fi
